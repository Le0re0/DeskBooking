<UserControl x:Class="DeskBooking.Views.BookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DeskBooking.Views"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <vm:BookingViewModel />
    </UserControl.DataContext>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Search TextBox -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="Search:" VerticalAlignment="Center"/>
            <TextBox Width="200" Margin="6,0,0,0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Bookings}" 
                  SelectedItem="{Binding SelectedBooking, Mode=TwoWay}" 
                  AutoGenerateColumns="False" Margin="0,0,0,6"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Employee" Binding="{Binding Employee.FullName}"/>
                <DataGridTextColumn Header="Desk" Binding="{Binding Desk.DeskNumber}"/>
                <DataGridTextColumn Header="From" Binding="{Binding BookedFrom, StringFormat=yyyy-MM-dd}"/>
                <DataGridTextColumn Header="Until" Binding="{Binding BookedUntil, StringFormat=yyyy-MM-dd}"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Left">
            <Button Command="{Binding AddCommand}" Margin="0,0,6,0" MinWidth="80">
                <TextBlock Text="Add" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
            <Button Command="{Binding EditCommand}" Margin="0,0,6,0" MinWidth="80">
                <TextBlock Text="Edit" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
            <Button Command="{Binding DeleteCommand}" MinWidth="80">
                <TextBlock Text="Delete" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
